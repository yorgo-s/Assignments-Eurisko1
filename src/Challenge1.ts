function reverseString(str: string): string {
  return str.split("").reverse().join(""); //split the words then reverse from the last character to the first and then join them back together
}
console.log("Challenge 1");
console.log(reverseString("yorgo semaan")); //naames ogroy
