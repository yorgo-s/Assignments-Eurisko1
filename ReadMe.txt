Name: Yorgo Semaan
I organized the assignment by creating a dedicated src folder for my TypeScript files.
These files are automatically compiled into JavaScript within the public folder using the tsc -watch command.
The resulting output is then displayed in the HTML page's console.